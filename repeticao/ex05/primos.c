#include <stdio.h>
#include <stdlib.h>

// descubra os numeros primos de entre 0 e 50;

void main()
{
    int i;

    /*for(i = 1; i <= 50; i++)
    {
        if( (i % i == 0) && (i % 1 == i))
            printf("\n%d Esse numero e primo", i);

        else
            printf("\n%d Esse numero NAO e primo", i);
    }*/
}

//Para essa questão tem que criar uma variavel que ira fzer duas contagens para divisoes que darão resto zero
//que será apenas os numero divisiveis por 1 e por ele mesmo.